import { CafeteriaProductos } from './cafeteria-productos';

describe('CafeteriaProductos', () => {
  it('should create an instance', () => {
    expect(new CafeteriaProductos()).toBeTruthy();
  });
});
